# Backend API base URL (no trailing slash).
# Dev: leave empty to use Vite proxy (/api → localhost:8080), or set for a remote backend.
# Production deploy: set VITE_API_BASE_URL on your host (Railway, Vercel, etc.) at build time.
VITE_API_BASE_URL=
